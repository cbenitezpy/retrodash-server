apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: retrodash-bridge-data
  labels:
    app.kubernetes.io/name: retrodash-bridge
    app.kubernetes.io/part-of: retrodash
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
